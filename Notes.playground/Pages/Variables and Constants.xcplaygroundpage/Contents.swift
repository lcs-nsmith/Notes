import Cocoa
// A "variable" is a place to store a single piece of information
// We can find the "data type" for a variable by cmd+lftclk the variable's name
// This is a string data type (stores 'abc')
var greeting = "Hello, playground"

// Create a variable names "age", which has a data type of Int
// Int data type Stores values like -7,  0, 15, etc
var age = 44

// Create a variable named "balance", which has a data type of Double
// The Double data type stores values with decimal portions
var balance = 1229.0

//Constants are just like variables execpt that once set, the value cannot change
let apporximatelyPi = 3.14
//approximatelyPi
